import {
  // create naive ui
  create,
  // component
  NButton, NLayout, NLayoutSider, NLayoutContent
} from 'naive-ui'

const naiveUI = create({
  components: [
    NButton, NLayout, NLayoutSider, NLayoutContent
  ]
})

export default naiveUI